import axios, {
  AxiosError,
  AxiosRequestConfig,
  AxiosRequestHeaders,
} from 'axios'

export class axiosRequestConfig implements AxiosRequestConfig {
  // CONFIGURE CLASS DEFAULTS HERE BY READING IN .ENV vars
}
